#include "NewLexer.h"

int perform_word_expansions1(t_complete_cmd *cmd)
{
	/*
	** TODO
	** 1) alias substitution
	** 2) ${} $
	** 3) ~
	** 4) $()
	** 5) $(())
	** Order may be wrong =)
	*/
	return (0);
}
